# Project Guidelines

## Frontend

When working on files under `resources/` (JS, CSS, or related assets), activate the `frontend` skill. It contains the project's frontend structure, naming conventions, component patterns, and organizational rules.
